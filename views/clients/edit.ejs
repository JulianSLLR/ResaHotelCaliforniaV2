<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier un client - Hôtel California</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-50">
    <nav class="bg-gray-800 text-white p-4">
        <div class="max-w-6xl mx-auto flex justify-between items-center">
            <h1 class="text-xl font-bold">Hotel California</h1>
            <ul class="flex gap-6">
                 <li><a href="/" class="hover:underline">Accueil</a></li>
                <li><a href="/chambres" class="hover:underline">Chambres</a></li>
                <li><a href="/reservations" class="hover:underline">Réservations</a></li>
                <li><a href="/clients" class="hover:underline">Clients</a></li>
            </ul>
        </div>
    </nav>

        <div class="max-w-6xl mx-auto p-6">
            <h1 class="text-2xl font-bold mb-4">Modifier un client</h1>

            <div class="bg-white border p-6">
                <form action="/clients/<%= client.idClient %>/edit" method="post">
                    <input type="hidden" name="idClient" value="<%= client.idClient %>">
                    <div class="mb-4">
                        <label class="block font-bold mb-1">Nom</label>
                        <input type="text" name="nom" value="<%= client.nom %>" required class="w-full border p-2">
                    </div>

                    <div class="mb-4">
                        <label class="block font-bold mb-1">Téléphone</label>
                        <input type="tel" name="telephone" value="<%= client.telephone %>" required class="w-full border p-2">
                    </div>

                    <div class="mb-4">
                        <label class="block font-bold mb-1">Email</label>
                        <input type="email" name="email" value="<%= client.email %>" required class="w-full border p-2">
                    </div>

                    <div class="mb-4">
                        <label class="block font-bold mb-1">Nombre de personnes</label>
                        <input type="number" name="nbPersonnes" value="<%= client.nbPersonnes %>" required class="w-full border p-2">
                    </div>

                    <div class="flex gap-2">
                        <a href="/clients" class="bg-gray-800 text-white px-4 py-2 hover:bg-gray-700">Annuler</a>
                        <button type="submit" class="bg-gray-800 text-white px-4 py-2 hover:bg-gray-700">Modifier</button>
                    </div>
                </form>
            </div>
        </div>

</body>

</html>