<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Supprimer une chambre - Hôtel California</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-amber-50 via-orange-50 to-amber-100 min-h-screen">
    <%- include('../partials/navbar') %>
    
    <div class="container mx-auto px-4 py-12">
        <div class="text-center mb-12">
            <h1 class="text-5xl font-bold text-amber-900 mb-4">🗑️ Supprimer une chambre</h1>
            <p class="text-xl text-amber-700">
                <%= chambre.numero %> - <%= chambre.capacite %> personne<%= chambre.capacite > 1 ? 's' : '' %>
            </p>
        </div>
        
        <div class="text-center">
            <form action="/chambres/<%= chambre.idChambre %>/delete" method="post">
                <button type="submit" class="bg-red-500 hover:bg-red-600 text-white font-bold py-3 px-8 rounded-lg shadow-lg transition duration-300 inline-flex items-center space-x-2">
                    <span>🗑️ Supprimer</span>
                </button>
            </form>
        </div>
    </div>
    <%- include('../partials/footer') %>
</body>
</html>