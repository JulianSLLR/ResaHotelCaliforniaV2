<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier une chambre - Hôtel California</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="flex flex-col min-h-screen bg-gray-50">
    <%- include('../partials/navbar') %>

        <div class="max-w-6xl mx-auto p-6 flex-1">
            <h1 class="text-2xl font-bold mb-4">Modifier une chambre</h1>

            <div class="bg-white border p-6">
                <form action="/chambres/<%= chambre.idChambre %>/edit" method="post">
                    <input type="hidden" name="idChambre" value="<%= chambre.idChambre %>">
                    <div class="mb-4">
                        <label class="block font-bold mb-1">Numéro</label>
                        <input type="text" name="numero" value="<%= chambre.numero %>" required class="w-full border p-2">
                    </div>

                    <div class="mb-4">
                        <label class="block font-bold mb-1">Capacité</label>
                        <input type="number" name="capacite" value="<%= chambre.capacite %>" required class="w-full border p-2">
                    </div>

                    <div class="mb-4">
                        <label class="block font-bold mb-1">Disponibilité</label>
                        <select name="disponibilite" class="w-full border p-2">
                            <option value="1" <%=chambre.disponibilite==1 ? 'selected' : '' %>>Disponible</option>
                            <option value="0" <%=chambre.disponibilite==0 ? 'selected' : '' %>>Occupée</option>
                        </select>
                    </div>

                    <div class="flex gap-2">
                        <a href="/chambres" class="bg-gray-800 text-white px-4 py-2 hover:bg-gray-700">Annuler</a>
                        <button type="submit" class="bg-gray-800 text-white px-4 py-2 hover:bg-gray-700">Modifier</button>
                    </div>
                </form>
            </div>
        </div>

        <%- include('../partials/footer') %>
</body>

</html>