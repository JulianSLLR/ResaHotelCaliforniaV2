<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Chambres - Hôtel California</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-amber-50 via-orange-50 to-amber-100 min-h-screen">
    <%- include('../partials/navbar') %>
    
    <div class="container mx-auto px-4 py-12">
        <div class="text-center mb-12">
            <h1 class="text-5xl font-bold text-amber-900 mb-4">🛏️ Nos Chambres</h1>
            <p class="text-xl text-amber-700">
                <%= chambres.length %> chambre<%= chambres.length > 1 ? 's' : '' %> disponible<%= chambres.length > 1 ? 's' : '' %>
            </p>
        </div>

        <div class="text-center mb-8">
            <a href="/chambres/new" class="bg-amber-600 hover:bg-amber-700 text-white font-bold py-3 px-8 rounded-lg shadow-lg transition duration-300 inline-flex items-center space-x-2">
                <span>➕</span>
                <span>Ajouter une chambre</span>
            </a>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% for (let chambre of chambres) { %>
                <div class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-2xl transition duration-300 border-2 border-amber-200">
                    <div class="bg-gradient-to-r from-amber-600 to-amber-700 text-white p-6">
                        <h2 class="text-3xl font-bold">Chambre <%= chambre.numero %></h2>
                    </div>
                    
                    <div class="p-6">
                        <div class="space-y-4">
                            <div class="flex items-center justify-between">
                                <span class="font-semibold text-amber-900">Capacité:</span>
                                <span class="text-amber-700 text-lg">
                                    <%= chambre.capacite %> personne<%= chambre.capacite > 1 ? 's' : '' %>
                                </span>
                            </div>
                            
                            <div class="flex items-center justify-between">
                                <span class="font-semibold text-amber-900">Statut:</span>
                                <% if (chambre.disponibilite == 1) { %>
                                    <span class="bg-green-100 text-green-800 px-4 py-2 rounded-full text-sm font-bold">
                                        ✓ Disponible
                                    </span>
                                <% } else { %>
                                    <span class="bg-red-100 text-red-800 px-4 py-2 rounded-full text-sm font-bold">
                                        ✗ Occupée
                                    </span>
                                <% } %>
                            </div>
                        </div>
                        
                        <div class="mt-6 grid grid-cols-3 gap-2">
                            <a href="/chambres/<%= chambre.idChambre %>" 
                               class="bg-amber-500 hover:bg-amber-600 text-white text-center py-2 rounded-lg transition duration-300 font-semibold">
                                👁️ Voir
                            </a>
                            <a href="/chambres/<%= chambre.idChambre %>/edit" 
                               class="bg-yellow-500 hover:bg-yellow-600 text-white text-center py-2 rounded-lg transition duration-300 font-semibold">
                                ✏️ Modifier
                            </a>
                            <a href="/chambres/<%= chambre.idChambre %>/delete" 
                               class="bg-red-500 hover:bg-red-600 text-white text-center py-2 rounded-lg transition duration-300 font-semibold">
                                🗑️ Suppr.
                            </a>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>

        <% if (chambres.length === 0) { %>
            <div class="bg-amber-100 border-l-4 border-amber-500 text-amber-800 p-6 rounded-lg text-center">
                <p class="font-bold text-xl mb-2">Aucune chambre disponible</p>
                <p>Commencez par ajouter une chambre en cliquant sur le bouton ci-dessus.</p>
            </div>
        <% } %>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>